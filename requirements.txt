# ⚠️ 버전을 == 로 고정해둔 이유 (2026-09-04)
#
# 예전엔 전부 `>=` 였어요. 그러면 배포할 때마다(도커 캐시가 한 번이라도 비면)
# discord.py나 pymongo가 예고 없이 새 버전으로 올라가고, 봇이 왜 깨졌는지
# 코드 변경 이력만 봐서는 절대 알 수 없게 돼요.
#
# 아래 버전은 2026-09-04 기준 **실제 프로덕션에서 정상 동작 중이던** 버전이에요
# (`flyctl ssh console -C "pip freeze"`로 직접 확인).
#
# 올릴 때는 한 번에 하나씩, 올린 뒤 실제로 돌려보고 올리세요.
discord.py==2.7.1
python-dotenv==1.2.3
aiohttp==3.14.3
pymongo==4.17.0
PyNaCl==1.6.2
davey==0.1.6
curl_cffi==0.16.3
cryptography==50.0.1

# 📌 yt-dlp는 여기 없어요 — 일부러예요.
#
# 유튜브가 추출 방식을 수시로 바꿔서, yt-dlp는 **낡으면 노래방이 그냥 깨져요.**
# 그런데 여기 적어두면 도커 레이어 캐시에 얼어붙어서 오히려 안 올라가요
# (실제로 2026-08-19 버전에 16일째 멈춰 있었어요).
#
# 그래서 Dockerfile 맨 뒤 `COPY . .` 다음 줄에서 따로 최신으로 설치해요.
# 코드가 바뀌면 그 레이어가 자동으로 무효화되니, 배포할 때마다 최신이 들어가요.
